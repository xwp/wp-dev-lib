{
  "name": "xwp/wp-dev-lib-tests",
  "minimum-stability": "dev",
  "scripts": {
    "test": [
      "rm -rf git && git init .",
      "git add .",
      "DIFF_BASE=HEAD vendor/xwp/wp-dev-lib/scripts/pre-commit"
    ],
    "post-install-cmd": [
      "rm -rf vendor/xwp/wp-dev-lib && ln -s ../../../../ vendor/xwp/wp-dev-lib"
    ],
    "post-update-cmd": [
      "rm -rf vendor/xwp/wp-dev-lib && ln -s ../../../../ vendor/xwp/wp-dev-lib"
    ]
  }
}
