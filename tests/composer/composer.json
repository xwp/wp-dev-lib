{
  "name": "xwp/wp-dev-lib-tests",
  "minimum-stability": "dev",
  "scripts": {
    "test": [
      "vendor/xwp/wp-dev-lib/scripts/pre-commit"
    ],
    "test:setup": [
      "rm -rf vendor/xwp/wp-dev-lib && ln -s $PWD/../.. vendor/xwp/wp-dev-lib",
      "rm -rf .git && git init . && git add phpcs.xml && git commit -m 'First commit'",
      "git checkout -b test && git add . && git commit -m 'Test commit'"
    ],
    "test:travis": [
      "DEV_LIB_PATH=vendor/xwp/wp-dev-lib/scripts . vendor/xwp/wp-dev-lib/scripts/travis.install.sh && . $DEV_LIB_PATH/travis.script.sh && . $DEV_LIB_PATH/travis.after_script.sh"
    ],
    "post-install-cmd": [
      "@test:setup"
    ],
    "post-update-cmd": [
      "@test:setup"
    ]
  },
  "require-dev": {
    "squizlabs/php_codesniffer": "^3.0",
    "wp-coding-standards/wpcs": "^0.14",
    "phpunit/phpunit": "^5"
  }
}
